function outTimes = onlyTimesDuringStep (inTimes, ampLetter)
  % returns a subset of times in ms that occurred during first step
  outTimes = [];
  if nargin < 2
     ampLetter = 'A'; 
  end
  
end